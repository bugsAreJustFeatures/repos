<html>
    <head>
        <title>
            Edit "<%= folderName %>" Page
        </title>
    </head>
    <body>

        <% if (errors.length > 0) { %>
            <ul>
                <% errors.forEach(err => { %>
                        <li>
                            <%= err.msg %>
                        </li>
                <% }) %>
            </ul>
        <% } %> 

        <form action="/<%= folderName %>/editName" method="post"> 
            <label for="newFolderName">New Folder Name: </label>
            <input type="text" name="newFolderName" id="newFolderName" required>

            <br>

            <button type="submit">Change Name</button>
        </form>

        <form action="/<%= folderName %>/delete" method="post">
            BE AWARE, THIS ACTION IS DANGEROUS AND CANNOT BE REVERTED.

            <button type="submit">DELETE FOLDER</button>
        </form>
    </body>
</html>